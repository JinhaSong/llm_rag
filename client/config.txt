# 수어 글로스 추천기 접속 설정
# '#' 로 시작하는 줄과 빈 줄은 무시된다.

# 서버 주소
#   - 사내망으로 접속할 때: 10.254.66.168
#   - 윈도우 PC 와 랜선 직결일 때: 192.168.100.2 로 바꿔 쓴다
host = 10.254.66.168

# 포트
#   8777 = ETRIKSL 1107 (표제어 1,087개)
#   8779 = 답변 핵심표제어 only (표제어 121개)
port = 8777

# 요청 경로 (분류 + 추천을 한 번에 처리하는 단일 엔드포인트)
path = /api/keywords

# 응답 대기 제한 (초). 등록된 질문은 0.1초 내, 신규 문장은 LLM 생성으로
# 수 초가 걸리고 드물게 20초를 넘기기도 한다.
timeout_sec = 60
